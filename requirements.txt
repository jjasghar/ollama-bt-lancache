# Python dependencies for Ollama BitTorrent Lancache client scripts
# Install with: pip install -r requirements.txt

# BitTorrent library
libtorrent>=2.0.0

# HTTP requests
requests>=2.28.0

# Path handling (Python 3.4+ has this built-in, but including for compatibility)
pathlib2>=2.3.0; python_version < "3.4"

# Optional: Better terminal output
colorama>=0.4.0; sys_platform == "win32"
tqdm>=4.64.0

# Optional: Configuration management
pyyaml>=6.0

# Optional: Logging improvements
rich>=13.0.0
